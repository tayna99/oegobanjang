# R2.1 — mock/실서버 전환 플래그(src/lib/api/config.ts). 기본값(파일 없음)은 mock이다 —
# 기존 프론트 테스트·8단계 데모 대본은 전부 mock 세계관을 전제로 한다.
# 실서버로 켜려면 이 파일을 .env.local로 복사하고 backend/를 로컬에서 띄운다(backend/README.md).
VITE_API_MODE=mock
VITE_API_BASE_URL=http://localhost:8000
