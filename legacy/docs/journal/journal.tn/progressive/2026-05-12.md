왜 이런 문제가 생겼나요?

# 실패 1, 2: tool_calls == 1 기대, 실제 4
구현 코드의 ReAct 루프 (정상 동작)
pythonfor _ in range(4):
    response = llm.invoke(messages)
    if not response.tool_calls:  # ← 빈 tool_calls 오면 루프 탈출
        break
    # tool 실행 후 다음 반복
이 구조는 실제 LLM이 "이제 끝났어요" 하면 tool_calls=[]를 돌려주기 때문에 정상입니다.
테스트의 mock 설정 (버그)
pythonllm_mock.bind_tools.return_value.invoke.return_value = ai_msg  # ← 문제
return_value는 몇 번을 호출해도 항상 같은 값을 반환합니다. ai_msg는 tool_calls가 채워진 상태니까, 루프가 break 조건을 절대 만족하지 못하고 4번을 꽉 채워 돕니다. 결과적으로 tool_results에 같은 fake 결과가 4개 쌓이고, tool_calls == 4가 되어 테스트가 실패합니다.

# 실패 3: RAG context가 메시지에 없음
테스트는 이렇게 기대합니다:
pythonstate.rag_contexts = [{"title": "체류연장 서류", "content": "여권 원본 필수"}]

# 메시지에 이 내용이 포함될 것
assert "체류연장 서류" in user_content
그런데 _run_document_priority_sub_agent 구현에서 state.rag_contexts를 아예 안 읽어요:
pythonmessages = [
    SystemMessage(content=_DOC_PRIORITY_SYSTEM_PROMPT),
    HumanMessage(content=f"근로자 ID: {worker_id}, 케이스 유형: {case_type}"),
    # ← rag_contexts가 없음
]

이것 때문에 실제 호출이 안 되는 건가요?
아닙니다. 이것들은 테스트 문제이지 런타임 문제가 아닙니다.
실제 LLM을 호출하면 마지막 응답에서 tool_calls=[]를 반환하기 때문에 루프가 정상 종료됩니다. 현재 315개 테스트가 통과하고 있다는 것도 런타임 로직이 올바르다는 증거입니다. 실패 원인은 순전히 mock 설정이 잘못된 것입니다.

어떻게 고치면 되나요?
실패 1, 2: side_effect로 바꾸기 (테스트 수정)
test_visa_sub_agents.py의 두 테스트에서 return_value → side_effect로 교체합니다.
TestVisaRiskSubAgent.test_tool_result_appended_when_assess_visa_risk_called
python# 기존 (버그)
llm_mock.bind_tools.return_value.invoke.return_value = ai_msg

# 수정
llm_mock.bind_tools.return_value.invoke.side_effect = [ai_msg, _ai_no_tool()]
TestDocumentPrioritySubAgent.test_tool_result_appended_when_assess_document_priority_called
python# 기존 (버그)
llm_mock.bind_tools.return_value.invoke.return_value = ai_msg

# 수정
llm_mock.bind_tools.return_value.invoke.side_effect = [ai_msg, _ai_no_tool()]
side_effect는 리스트를 받아서 **첫 번째 호출엔 ai_msg, 두 번째 호출엔 _ai_no_tool()**을 반환합니다. 두 번째 응답에서 tool_calls=[]이므로 루프가 즉시 탈출합니다.

실패 3: 선택지 2가지
선택지 A — 테스트를 제거 (빠르게 닫기)
테스트 자체가 현재 구현에 없는 기능을 기대하는 것이라면, test_rag_context_included_in_message 테스트를 제거하거나 skip 처리합니다.
선택지 B — 구현에 rag_contexts 추가 (권장)
테스트의 의도가 맞습니다. 서브에이전트가 이미 검색된 RAG 컨텍스트를 받지 못하면 자기 힘으로 search_policy_documents를 다시 호출해야 해서 LLM 호출이 낭비됩니다. _run_document_priority_sub_agent 구현을 이렇게 수정합니다:
pythondef _run_document_priority_sub_agent(
    state: ForeignHiringState,
    worker_id: str,
    llm_base: ChatOpenAI,
    case_type: str = "stay_extension",
) -> dict[str, Any]:
    llm = llm_base.bind_tools(_DOC_PRIORITY_TOOLS)

    # rag_contexts가 있으면 메시지에 추가
    rag_context_text = ""
    if state.rag_contexts:
        rag_lines = []
        for ctx in state.rag_contexts:
            title = ctx.get("title", "")
            content = ctx.get("content", "")
            grade = ctx.get("evidence_grade", "")
            rag_lines.append(f"[{grade}] {title}: {content}")
        rag_context_text = "\n\n참고 공식 문서:\n" + "\n".join(rag_lines)

    messages: list[Any] = [
        SystemMessage(content=_DOC_PRIORITY_SYSTEM_PROMPT),
        HumanMessage(
            content=f"근로자 ID: {worker_id}, 케이스 유형: {case_type}{rag_context_text}"
        ),
    ]
    # 이하 동일

요약
실패원인수정 위치난이도1, 2mock return_value가 무한 반복 → 루프 4회테스트 파일 2줄 수정5분3구현에 rag_contexts 반영 코드 없음구현 수정 (B) 또는 테스트 제거 (A)10분 (B) / 2분 (A)
셋 다 런타임 동작과는 무관하니 실제 API 호출에서 문제가 생기고 있다면 별도 원인을 봐야 합니다.