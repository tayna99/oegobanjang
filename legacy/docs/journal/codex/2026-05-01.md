# 2026-05-01 Codex / Claude 작업 로그

## 오늘 계획
- WorkBridge 저장소의 초기 실행 가능한 프로젝트 골격을 만든다.
- Python/uv 기반 개발 환경을 잡고, 이후 Agent OS 작업을 올릴 수 있는 최소 파일을 준비한다.
- 이후 날짜에 이어질 WorkBridge harness, RAG, phase 실행의 출발점을 만든다.

## 오늘 결정한 것
- 저장소 초기 형태는 작게 시작하고, `README.md`, `pyproject.toml`, `uv.lock` 중심으로 고정한다.
- 의존성 관리는 `uv.lock`으로 추적한다.
- 이후 작업은 별도 문서와 phase/mission 단위로 확장한다.

## 오늘 실행한 것
- `Initial project setup` 커밋으로 기본 프로젝트 파일을 추가했다.
- `.gitignore`, `.python-version`, `README.md`, `main.py`, `pyproject.toml`, `uv.lock`을 만들었다.
- `Update uv lockfile` 커밋으로 `uv.lock`을 갱신했다.

## 오늘 Codex에 물어본 것
### Q1.
WorkBridge를 시작하려면 어떤 최소 골격이 필요한가.

A.
초기 저장소는 실행 환경과 README, Python 프로젝트 설정을 먼저 갖추고, 이후 agent/runtime/RAG 작업을 단계적으로 올리는 방식이 적합하다.

결정/액션:
- 초기 프로젝트 파일 생성
- uv lockfile 갱신
- 이후 phase 기반 작업의 기준점 마련

## Claude가 한 것
- 이 날짜의 로컬 Git 기록에서는 Claude 공동 작업 흔적이 명시적으로 확인되지 않았다.
- Claude 관련 작업은 이후 `.claude/` 설정과 프롬프트/명령 템플릿 정리 단계에서 본격적으로 확인된다.

## 근거
- Git commit `203fe52`: `Initial project setup`
- Git commit `2896f9a`: `Update uv lockfile`

## 다음에 이어서 할 것
- 프로젝트 목적과 Agent Runtime 방향을 문서화한다.
- 폴더 구조, 작업 규칙, 검증 명령을 정리한다.
- Codex/Claude가 같은 기준으로 일할 수 있는 지침 문서를 만든다.
