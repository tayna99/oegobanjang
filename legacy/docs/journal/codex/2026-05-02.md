# 2026-05-02 Codex / Claude 작업 로그

## 오늘 계획
- WorkBridge를 외국인 고용 운영 OS로 확장할 수 있는 monorepo 골격을 잡는다.
- `backend/`, `data-pipeline/`, `evals/`, `frontend/`, `infra/`, `missions/`, `docs/` 기준의 새 구조를 정리한다.
- AI/사람 개발자가 함께 따를 수 있는 공통 작업 지침과 mission 체계를 만든다.

## 오늘 결정한 것
- 새 구조는 `backend`가 제품 API와 Agent Runtime을 함께 담당하고, `data-pipeline`은 RAG 수집/전처리, `evals`는 평가 데이터와 리포트를 담당한다.
- Agent Runtime 관련 코드는 `backend/app/agent_runtime/` 아래에 둔다.
- 실행 단위는 예전 phase 문서만이 아니라 `missions/active/*.md` 중심으로 옮겨갈 수 있게 준비한다.
- 폴더 구조는 `FOLDER_STRUCTURE.md`를 기준 문서로 삼는다.

## 오늘 실행한 것
- `Align project scaffold with folder structure` 커밋으로 대규모 프로젝트 골격을 추가했다.
- `.claude/` 명령 템플릿과 역할별 프롬프트를 추가했다.
- `AGENTS.md`, `CLAUDE.md`, `FOLDER_STRUCTURE.md`, `README.md` 등 루트 문서를 정리했다.
- FastAPI 백엔드, Agent Runtime, RAG, tools, schemas, evals, frontend, infra, missions 폴더를 만들었다.
- `scripts/verify_all.sh`, `scripts/run_backend_tests.sh`, `scripts/run_agent_tests.sh`, `scripts/run_frontend_tests.sh`, `scripts/run_evals.py` 검증 스크립트 골격을 추가했다.

## 오늘 Codex에 물어본 것
### Q1.
외국인 고용 Agentic OS를 어떤 폴더 구조와 작업 단위로 나누는 게 좋은가.

A.
제품 API, Agent Runtime, RAG 데이터 파이프라인, 평가 하네스, 프론트엔드, 인프라를 분리한 monorepo 구조가 적합하다.

결정/액션:
- 새 monorepo 구조 생성
- mission 기반 작업 지시서 추가
- 검증 스크립트와 문서 골격 추가

## Claude가 한 것
- `.claude/CLAUDE.md`를 추가했다.
- `.claude/commands/` 아래에 `plan-feature`, `implement-feature`, `verify-feature`, `review-pr` 명령 템플릿을 추가했다.
- `.claude/prompts/` 아래에 backend, frontend, agent-runtime, rag, qa, reviewer 역할별 프롬프트를 추가했다.

## 근거
- Git commit `685b735`: `Align project scaffold with folder structure`

## 다음에 이어서 할 것
- Agent Runtime, RAG, Evidence Log, backend API, frontend dashboard를 mission 단위로 구체화한다.
- seed/eval 데이터를 채워서 실제 테스트 가능한 thin slice를 만든다.
- Codex와 Claude가 생성한 작업이 같은 문서 계약을 따르는지 계속 점검한다.
