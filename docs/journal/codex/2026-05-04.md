# 2026-05-04 Codex / Claude 작업 로그

## 오늘 계획

- 기존 WorkBridge RAG/Phase 1C 작업을 새 `oegobanjang` monorepo 구조에 맞춰 이식한다. (예전 폴더 구조의 RAG 코드를 새 `backend/`, `data-pipeline/`, `evals/` 구조로 옮기는 작업)
- Codex CLI와 PowerShell profile 로깅 흐름을 점검한다. (Codex 명령 실행과 작업 로그 저장이 제대로 되는지 확인)
- 새 구조에서 공통 schema, state machine, eval metric, evidence/audit log, guardrail 기준을 다시 문서와 코드로 고정한다. (여러 agent가 같은 규칙으로 움직이도록 계약 문서와 안전 코드를 정리)

## 오늘 결정한 것

- 예전 `data/raw/` 원본은 바로 섞지 않고, `data-pipeline/raw/`로 옮기는 raw migration을 별도 작업으로 둔다. (원본 문서 이식은 충돌 위험이 있어 별도 단계로 분리)
- 새 구조의 실행 단위는 `phases/`보다 `missions/active/*.md`가 중심이다. (예전 phase 파일 대신 mission 파일을 실제 작업 지시서로 사용)
- 예전 phase 의사결정 중 계속 유효한 것은 새 구조의 `docs/*.md` 계약 문서로 다시 옮긴다. (과거 설계 중 필요한 원칙만 새 문서 체계에 반영)
- Guardrail은 문서 Non-Goals에만 두지 않고 코드 레벨에서 차단한다. (금지 작업을 설명만 하지 않고 실제 코드에서 blocked 처리)
- Evidence/Audit Log는 append-only event source 성격으로 보고, silent fallback 대신 `blocked` 또는 명시적 reason을 남긴다. (중요 판단 기록은 덮어쓰지 않고, 실패는 조용히 넘어가지 않게 처리)

## 오늘 실행한 것

- `port-rag-indexing-new-structure` 브랜치에서 RAG runtime 코드를 새 구조로 포트했다. (RAG 검색 코드가 새 backend 위치에서 동작하도록 옮김)
- `.gitignore`에 generated cache, index, timestamped report ignore 정책을 반영했다. (자동 생성되는 캐시/인덱스/시간별 리포트가 Git에 무분별하게 올라가지 않게 함)
- `backend/app/agent_runtime/rag/` 아래 chunking, embeddings, retriever, citation, vector_store, evaluate 코드를 추가했다. (문서 쪼개기, 임베딩, 검색, 출처 표시, 벡터 저장소, 평가 실행 코드 추가)
- `backend/tests/test_rag_indexing.py`, `backend/tests/test_rag_eval_dataset.py`를 추가했다. (RAG chunk와 eval 데이터가 깨지지 않는지 검증하는 테스트 추가)
- `data-pipeline/seed/sample_policy_docs.jsonl`과 `data-pipeline/processed/chunks/*.jsonl`을 갱신했다. (샘플 정책 문서와 검색용 chunk 산출물을 새 구조에 맞게 갱신)
- `evals/datasets/rag_retrieval_cases.jsonl`과 최신 eval report를 추가했다. (검색 품질을 확인할 질문/기대 source 데이터와 평가 결과 추가)
- Codex CLI `0.128.0` 실행, `codex exec` 단순 응답, profile 로깅을 점검했다. (Codex CLI가 설치되어 있고 기본 명령이 동작하는지 확인)
- `.codex\sessions` 권한 문제를 확인하고 ACL 재설정을 수행했다. (Codex 세션 로그 폴더 접근 권한 문제를 Windows 권한 설정으로 해결)
- `docs/SCHEMA_CONTRACT.md`, `docs/STATE_MACHINE.md`, `docs/EVAL_METRICS.md`를 추가했다. (공통 JSON 계약, 상태 흐름, 평가 지표 기준 문서 추가)
- `docs/EVIDENCE_LOG_SCHEMA.md`, `docs/OBSERVABILITY.md`를 업데이트했다. (Evidence Log와 관측/추적 필드 기준을 새 runtime에 맞게 갱신)
- `backend/app/agent_runtime/guardrails.py`와 `backend/tests/test_guardrails.py`로 Guardrail as Code를 구현했다. (금지 행동을 코드와 테스트로 강제)
- `uv run pytest backend/tests/test_guardrails.py`, `uv run pytest`를 실행해 guardrail 단위 테스트와 전체 backend 테스트 통과를 확인했다. (안전 정책 테스트와 전체 백엔드 테스트가 통과하는지 확인)

## 오늘 Codex에 물어본 것

### Q1. Phase 1C를 다시 실행하고 새 구조에 맞춰 어떻게 정리해야 하는가. (예전 RAG 작업을 새 monorepo에 어떻게 옮길지 질문)

A. 예전 구조를 통째로 되살리기보다, 새 monorepo 구조 위에 RAG runtime, seed/eval, processed chunks, reports만 우선 이식하고 raw migration은 별도 작업으로 분리하는 것이 안전하다. (전체 복원보다 필요한 실행 자산만 먼저 옮기는 방식이 안전하다는 판단)

결정/액션:

- `backend/app/agent_runtime/rag/`로 런타임 코드 이식. (서비스에서 실제 RAG 검색을 실행할 코드 위치)
- `data-pipeline/`과 `evals/`로 데이터/평가 산출물 분리. (데이터 준비와 평가를 backend 코드와 분리)
- raw 원본 이식은 별도 migration으로 보류. (원본 문서 이동은 다음 단계로 분리)

### Q2. Codex CLI와 profile 로깅이 정상 동작하는가. (개발 도구 실행과 로그 저장 상태 확인)

A. `codex --version`, `where.exe codex`, `codex exec` 단순 응답을 확인했고, profile test 응답도 기록했다. 일부 로그는 인코딩이 깨졌지만 실행 자체는 확인되었다. (도구는 동작하지만 Windows 한글/인코딩 로그는 주의 필요)

결정/액션:

- Codex CLI `0.128.0` 확인. (사용 중인 Codex CLI 버전 확인)
- `docs/journal/codex/*.txt`에 timestamped 실행 로그 저장. (시간별 실행 로그를 journal 하위에 보관)
- profile에서 `cdx`, `jnl`, `cdx-last` 흐름 확인. (자주 쓰는 PowerShell alias가 동작하는지 확인)

### Q3. 예전 phase 문서와 새 mission 구조의 관계를 어떻게 봐야 하는가. (과거 phase 파일과 현재 mission 파일의 역할 정리)

A. 예전 phase 문서는 백업 브랜치와 stash에 보존되어 있고, 현재 새 구조에서는 `missions/active/*.md`가 active spec 역할을 한다. (현재 실행 기준은 mission 문서이고 phase는 참고/아카이브)

결정/액션:

- 예전 phase를 그대로 복원하지 않고 필요한 의사결정만 새 docs 계약으로 이식. (과거 문서를 통째로 되살리지 않고 핵심 규칙만 반영)
- `backup/main-before-oegobanjang-sync`와 `stash@{0}`를 참고 보관소로 유지. (이전 작업 자산을 잃지 않도록 보존)

## Claude가 한 것

- 5월 4일 Git 커밋과 로컬 작업 로그에서는 Claude 공동 저자 표기가 새로 확인되지는 않았다. (이 날짜의 직접 공동 커밋 증거는 없음)
- 다만 5월 2~3일에 정리된 `.claude/` 명령/프롬프트 체계와 Claude 공동 생성 seed 데이터가 이날 작업의 입력 자료로 이어졌다. (이전 Claude 작업 결과가 오늘 작업의 기반으로 사용됨)
- `docs/journal/history.md`에 남아 있는 Notion/기획 업데이트 기록은 Claude와 병행해 관리해야 할 제품 의사결정 자료로 보인다. (제품 방향과 리스크 기준은 별도 기록으로 계속 참고 필요)

## 근거

- Git commit `a91d781`: `chore: ignore generated cache, index, and timestamped reports`. (자동 생성 파일 ignore 정책 커밋)
- Git commit `a6481e3`: `rag: port chunking/embeddings/retriever/citation/vector_store + evaluate`. (RAG runtime 코드 이식 커밋)
- Git commit `e84f59f`: `tests: add RAG indexing and eval dataset tests`. (RAG 테스트 추가 커밋)
- Git commit `cce8efa`: `data: regenerate chunks JSONL + update seed sample`. (chunk 산출물과 seed 데이터 갱신 커밋)
- Git commit `613c2cf`: `evals: update RAG retrieval cases`. (RAG 검색 평가 케이스 갱신 커밋)
- Git commit `f7398c8`: `evals: latest Phase 1C reports (Hit@3 evidence)`. (Hit@3 평가 결과 증거 커밋)
- `docs/journal/2026-05-04.md`. (5월 4일 종합 작업 리포트)
- `docs/journal/codex/2026-05-04_100952.txt`. (Codex 실행 로그 파일)
- `docs/journal/codex/2026-05-04_101744.txt`. (Codex 실행 로그 파일)
- `docs/journal/codex/2026-05-04_102353.txt`. (Codex 실행 로그 파일)

## 다음에 이어서 할 것

- `backup/main-before-oegobanjang-sync:data/raw/*`를 `data-pipeline/raw/`로 옮기는 raw migration을 별도 커밋으로 처리한다. (백업 브랜치의 원본 문서를 새 raw 위치로 안전하게 이전)
- `source_manifest.json` 경로를 새 구조 기준으로 갱신한다. (원본 문서 목록이 새 `data-pipeline/raw/` 경로를 가리키게 수정)
- Phase 2A PII middleware와 Phase 3 state machine/Evidence Log 구현으로 이어간다. (개인정보 마스킹, 상태 전이, 감사 로그를 다음 단계로 구현)
